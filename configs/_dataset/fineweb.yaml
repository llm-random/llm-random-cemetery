defaults:
  - default
  - _self_

trainer:
  train_dataloader:
    datasets:
      ${cluster_switch.train_path_fineweb}: 1.0
    dataset_split: train
    num_workers: 4

  eval_dataloader:
    datasets:
      ${cluster_switch.eval_path_fineweb}: 1.0
    dataset_split: train
    num_workers: 4