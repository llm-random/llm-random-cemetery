# _run_: true

# TODO missing llama tokenizer

infrastructure:
  metric_logger:
    name: test
    project_name: pmtest/llm-random
    tags:
      - nano
      - pc
      - test_save

defaults:
  - _cluster@_here_: entropy_4
  - _model@_here_: llama_3_1
  - _trainer@_here_: default
  - _dataset@_here_: c4
  - _checkpoints@_here_: mas/hf_llama_3_1.yaml
  - _misc@_here_: default

trainer:
  n_steps: 0
  learning_rate: 0.00005
  gradient_accumulation_steps: 4

  checkpoint:
   save:
    interval: -1
    path: checkpoints
    model_checkpoint_filename: model_checkpoint_filename.pt
    training_state_filename: training_state_filename.pt
