parent: configs/baselines/gpt/dense/base.yaml
md5_parent_hash: 763f39978a58f704200275285162594c
time: "40:00:00"
n_gpus: 1

params:
  # tokenizer: gpt

  name: "base_tune_lr"
  # fsdp_enabled: true
  mixed_precision: true
  mixed_precision_dtype: bfloat16
  flash_attention: true
  # fsdp_modules_to_wrap: "TransformerBlock,EmbeddingLayer,PredictionHead"
  # activation_checkpointing_modules: "TransformerBlock,EmbeddingLayer,PredictionHead"
  gradient_accumulation_steps: 4
  grad_clip: 1.0

  lr_ratio_modules: ["head", "embedding_layer", ]
  lr_ratios: [0.2, 5]
  tags: ["relative_lr", "base", "lr_grid"]

  n_steps: 37500
  ^learning_rate: [1e-3, 2e-3, 5e-3] 
  lr_warmup_steps: 375
  final_lr_step: 37500
  weight_decay: 0.01
  scheduler: "cosine"
  decoding_interval: 0
  logging_interval_heavy: 10000
  init_type: truncated_normal
  init_scale: 0.1
  save_weights_interval: 0
  batch_size: 128
  cutoff: 1024
  dataset_type: c4
