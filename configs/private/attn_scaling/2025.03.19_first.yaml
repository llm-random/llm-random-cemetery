runner: "research.attention_moe.train.train"
time: 1-00:00:00
n_gpus: 2
interactive_debug: false
backends_module: research.attention_moe.backends

# train params
params:
  name: "debug_diff"

  # base bass
  mixed_precision: true
  flash_attention: true

  # logging etc.
  logger_types: "neptune"
  project_name: "pmtest/llm-random"
  logging_interval_heavy: 1000
  logging_interval_light: 500
  logging_interval_loss: 1000
  # save_weights_path: "model_ckpt"
  save_weights_interval: 15000
  tags: ["2025_03_08_grid_shadow_copy"]


  cutoff: 2048
  batch_size: 64
  n_steps: 45_000
  final_lr_step: 45_000
  scheduler: cosine
  lr_warmup_steps: 4500
  final_lr_fraction: 0.1

  model_type: gpt
  dataset_type: c4

  
  # model
  use_rope: true

  # lowrank_dtype: float32
  ^dmodel: [768]
  n_blocks: 12
  n_att_heads: 12
  data_seed: 42
  init_type: truncated_normal
  init_scale: 0.1
  mixed_precision_dtype: bfloat16
  # ^n_kv_heads: [12]

  # ^learning_rate: [5e-4, 1e-3, 2e-3, 5e-3]

  rope_theta: 10000
  ff_mode: swi_glu
  use_final_norm: true

  ^dff: [
    2388
  ]
  ^attention_mode: [
    differential
  ]
  ^n_kv_heads: [4]
  ^learning_rate: [2e-3, 5e-4]
  # save_weights_path: "/storage_ssd_1/mpioro/checkpoints_better_diff"



  # fsdp
  fsdp_enabled: true
  fsdp_modules_to_wrap: "TransformerBlock,EmbeddingLayer,PredictionHead"
  fsdp_selective_precision_modules: "AttentionMechanism,RoPE"
  torch_compile: false
  # log_gradients_and_weights: true