Write neat, readable, and efficient code.
Prioritize correctness and functionality — the code must work.
Avoid unnecessary complexity and minimize security risks.
Be concise.

Each project has it's own folder in \src which should be independent of other folders in src, except src\core.